package com.simple.chris.pebble;

import android.content.Context;
import android.graphics.Color;
import android.util.AttributeSet;
import android.util.DisplayMetrics;
import android.util.TypedValue;
import android.view.Gravity;
import android.widget.LinearLayout;

import androidx.annotation.Nullable;
import androidx.appcompat.widget.ViewUtils;

/*public class LinearLayoutShadow extends LinearLayout {
    public LinearLayoutShadow(Context context) {
        super(context);
        initBackground();
    }

    public LinearLayoutShadow(Context context, @Nullable AttributeSet attributeSet) {
        super(context, attributeSet);
        initBackground();
    }

    public LinearLayoutShadow(Context context, @Nullable AttributeSet attributeSet, int defStyleAtt) {
        super(context, attributeSet, defStyleAtt);
        initBackground();
    }

    private void initBackground() {
        setBackground(ViewUtils.generateBackgroundWithShadow(this, Color.parseColor("#ffffff"), TypedValue.applyDimension(TypedValue.COMPLEX_UNIT_DIP,
                20, getResources().getDisplayMetrics())), Color.parseColor("7d7d7d"), TypedValue.applyDimension(TypedValue.COMPLEX_UNIT_DIP,
                8, getResources().getDisplayMetrics()), Gravity.BOTTOM);
    }
}*/
