package com.simple.chris.pebble.testing

class SmallScreen {
    /**
     * This would act as the 'smallScreenFragHolder' from activity_main.xml
     *
     * It would display the Settings and About screens, maybe as a dialog?
     * Making this it's own thing cleans up the logic as it would only need
     * to be called rather than ActivityMain.kt handling all the logic.
     */
}